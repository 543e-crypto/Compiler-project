def decrement(x):
  return x - 1
fed

def increment(x):
  return x + 1
fed

def compute(x,y) :
  sum = y
  while x > 0:
    sum = increment(sum)
    x = decrement(x)
  elihw
  return sum
fed

print (compute(compute(25,25),compute(25,25))) !should print 100


